# Dependências do app de controle de gado.
# Versões mínimas (>=) para resolver wheels compatíveis com a versão do Python instalada.
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0.36
pydantic>=2.10
pydantic-settings>=2.7
jinja2>=3.1
python-multipart>=0.0.20
openpyxl>=3.1
# Banco: SQLite (local, embutido no Python) agora.
# psycopg só é necessário ao subir para Postgres/Supabase:
psycopg[binary]>=3.2
pytest>=8.3
httpx>=0.28
